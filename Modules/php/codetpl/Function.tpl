	/**
	* {RULE_REF_TYPE}_{RULE_REF_ID}
	* Specification : {RULE_REF_SPECID} {RULE_REF_SPECTITLE} 
	* {RULE_REF_COMMENT}
	* 
	* @version {RULE_INFO_VERSION} 
	* @since {RULE_INFO_SINCE} 
	* @author {RULE_INFO_AUTHORS}
	*/
	
	public function rule_{RULE_ID}( $assertID_int)
	{
		$v1 = true;
		switch( $assertID_int)
		{
		{CASE_TPL}
		}
	}